create table REVINFO (
REV integer generated by default as identit
y (start with 1),
REVTSTMP bigint,
primary key (REV)
);



i have a table design like this.

Role table —- ROLE_PRIVILEGE table —- PRIVILEGE table

Role table has this code

@Audited
@AuditMappedBy(mappedBy = “role”)
@OneToMany(cascade = { CascadeType.ALL }, fetch = FetchType.LAZY, mappedBy = “role”)
private Set rolePrivileges;

ROLE_PRIVILEGE table has this code

@Audited
@ManyToOne(cascade = { CascadeType.DETACH }, fetch = FetchType.LAZY, optional = false)
@JoinColumn(name = “PRIVILEGE_ID”)
private ApplicationPrivilege privilege;

i can edit role information like name, desc and add/remove one or more privileges.
the added edited privileges references use to store in ROLE_PRIVILEGE table.

when i am add/edit role information like name, desc and add/remove one or more privileges…. role Audited working fine for name and desc but for AuditMappedBy its not working.

when i am deleting the role entry AuditMappedBy is working.

@AuditJoinTable
@AuditMappedBy
@AuditOverride
@NotAudited
@RevisionTimestamp
@RevisionNumber
@
@
@